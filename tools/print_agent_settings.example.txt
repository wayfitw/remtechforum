# Настройки агента печати. Скопируйте файл как settings.txt рядом с программой.
# Строки с решёткой — комментарии.

# Ключ доступа к очереди печати. Выдаётся отдельно, в репозиторий не кладём.
PRINT_QUEUE_KEY=

# Адрес сайта киоска.
BASE_URL=https://remtech-forum.ru

# Имя принтера, как оно видно в «Принтеры и сканеры». Пусто — агент сам найдёт
# принтер, в названии которого есть QW410 или DNP.
PRINTER_NAME=

# 1 — пробный режим: карточки скачиваются и закрываются, но на бумагу не идут.
DRY_RUN=0
